<%= form_with(model: webpush_subscription, url: url, class: "ctmf", data: { turbo: false }) do |form| %>
  <%= render "shared/error_messages", instead: webpush_subscription %>

  <div>
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :status, style: "display: block" %>
    <%= form.select :status, WebpushSubscription.statuses.keys.map { |k| [t("activerecord.enums.webpush_subscription.status.#{k}"), k] }, {} %>
  </div>

  <div>
    <%= form.submit "送信" %>
  </div>
<% end %>
